RewriteEngine On

# Kalau mau buat SPA-like (semua route ke index.html kecuali file nyata & PHP)
# Tapi karena kamu punya PHP terpisah, cukup ini saja untuk security dasar:
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]

# Atau kalau tidak mau SPA, hapus saja bagian rewrite di atas.

# Tambahan security:
<Files "config.php">
    Order allow,deny
    Deny from all
</Files>